/**
 * To determine a var is an set or not empty set
 * @param set var to determine
 * @param length length equals true ? not empty set : set
 */
export declare const settable: (set?: any, length?: boolean) => boolean;
